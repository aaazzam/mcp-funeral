[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcp-funeral"
version = "0.1.0"
description = "RIP MCP"
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = ["typer>=0.9"]

[project.scripts]
mcp-funeral = "mcp_funeral:main"
